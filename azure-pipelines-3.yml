trigger:
  - main
pool:
 vmImage: ubuntu-latest

stages:
   - stage: Build
     jobs:
       - job: BuildPush
         steps:
         - task: Docker@2
           displayName: Build And Push
           inputs:
              containerRegistry: 'acr-connection'
              repository: 'dhanuregistry'
              command: 'buildAndPush'
              Dockerfile: '**/quote-generator-app-2/Dockerfile'
              tags: |
                v1
     