trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
   imageName: 'myapp'
   acrLoginServer: 'myappregistry123.azurecr.io'

stages:
  - stage: BuildAndPush
    displayName: Build and Push Docker image to ACR
    jobs:
      - job: DockerBuildPush
        steps:
          - checkout: self
          #Login To ACR
          - task: Docker@2
            displayName: Login to ACR
            inputs:
              command: login
              containerRegistry: 'acr-connection'

          # Build Docker Image
          - task: Docker@2
            displayName: Build Docker Image
            inputs:
              command: build
              repository: $(acrLoginServer)/$(imageName)
              Dockerfile: '**/Dockerfile'
              tags: |
                 latest

          # Push Docker Image
          - task: Docker@2
            displayName: Push Docker Image
            inputs:
              command: push
              repository: $(acrLoginServer)/$(imageName)
              tags: |
                latest
              containerRegistry: 'acr-connection'
